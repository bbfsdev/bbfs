#!/usr/bin/env ruby

# Runs content server. This server monitors a set of directories (blobs/patterns).
# The files in those directories are indexed (calculating their SHA1).
# Each unique content is backed up to the remote (backup) server.

require 'params'
require 'file_monitoring/file_monitoring'
require 'file_utils/file_generator/file_generator'
require 'run_in_background'
require 'testing_server/testing_server_same_machine'

Params.init ARGV

RunInBackground.run { TestingServer.run_testing_server }
